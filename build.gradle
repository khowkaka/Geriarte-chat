buildscript {
    repositories { google(); mavenCentral() }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.5.2'
    }
}
allprojects {
    // managed by settings.gradle
}
tasks.register('clean', Delete) {
    delete rootProject.buildDir
}